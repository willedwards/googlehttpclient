The NoteUrlConverter is the class that needs to be implemented by the caller.
Since the caller must supply information about how to "do stuff".

Local deploy:
mvn appengine:devserver

GAE deploy:
mvn appengine:update
